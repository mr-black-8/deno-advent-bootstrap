/**
 * Make a copy of this file for each day e.g. "01_day.ts"
 * run `deno task day <day>` to execute
 */

// Implement & return solution one here, solutions have historically been a number
export const solutionOne = (input: string[]): number => {
  let result = 0;
  input.forEach(line => result += Number(line));


  return result;
};

// Implement & return solution two here, solutions have historically been a number
export const solutionTwo = (input: string[]): number => {
  let result = 0;
  input.forEach(line => result += Number(line));


  return result;
};
